This directory contains utilities meant to be installed to containers.
It will be copied by the makefile in the docker build context before building

one subdirectory per architecture, "all" to be copied to all architectures
